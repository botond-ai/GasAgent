# Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Required: OpenAI API key for generating embeddings and LLM responses
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Override default settings
# EMBEDDING_MODEL=text-embedding-3-small
# LLM_MODEL=gpt-4o-mini
# CHROMA_DB_PATH=./chroma_db
# COLLECTION_NAME=prompts

# ============================================================
# Jira Integration (Optional)
# ============================================================
# Enable Jira task creation directly from the RAG system
# If all three settings are provided, Jira integration will be enabled

# Your Jira instance URL (e.g., https://your-company.atlassian.net)
# JIRA_BASE_URL=https://your-company.atlassian.net

# Your Jira account email
# JIRA_EMAIL=your.email@company.com

# Jira API token (generate at: https://id.atlassian.com/manage-profile/security/api-tokens)
# JIRA_API_TOKEN=your_jira_api_token_here

# Department to Jira project mapping (comma-separated: department:PROJECT_KEY)
# Default: hr:HR,dev:DEV,support:SUP,management:MGT
# JIRA_DEPARTMENT_MAPPING=hr:HR,dev:DEV,support:SUP,management:MGT

# Use MCP for Jira operations (true/false)
# MCP provides duplicate detection and better error handling
# Falls back to REST API if MCP is unavailable
# USE_JIRA_MCP=true

# ============================================================
# Microsoft Teams Integration (Optional)
# ============================================================
# Send notifications to Teams channels when Jira tickets are created
# Department to Teams webhook URL mapping (comma-separated: department:webhook_url)
# Get webhook URLs from Teams: Channel -> Connectors -> Incoming Webhook
# TEAMS_WEBHOOKS=hr:https://outlook.office.com/webhook/xxx,dev:https://outlook.office.com/webhook/yyy,support:https://outlook.office.com/webhook/zzz
