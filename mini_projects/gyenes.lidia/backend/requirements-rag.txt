# Optional dependencies for RAG (Retrieval-Augmented Generation)
# Install these when you're ready to use production vector database and document ingestion
#
# Installation:
#   pip install -r requirements-rag.txt
#
# These are NOT needed for basic mock RAG functionality

# Vector Database
qdrant-client==1.7.0

# LangChain ecosystem for document processing
langchain==0.1.0
langchain-openai==0.0.2
langchain-community==0.0.10

# Document loaders
pypdf==3.17.4              # PDF support
python-docx==1.1.0         # DOCX support
unstructured==0.11.8       # Markdown and other formats
markdown==3.5.1            # Markdown parsing

# Text processing
tiktoken==0.5.2            # Token counting for OpenAI models
